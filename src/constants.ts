/**
 * Default color scheme for map regions
 */
export const DEFAULT_FILL_COLOR = "#B2C3EE";
export const DEFAULT_HOVER_COLOR = "#819feb";
export const DEFAULT_ACTIVE_COLOR = "#325ECD";
export const DEFAULT_STROKE_COLOR = "#FFFFFF";
export const DEFAULT_LEADER_COLOR = "#717C8C";

/**
 * SVG viewBox configuration
 * Format: "minX minY width height"
 */
export const VIEWBOX = "40 10 890 600";

/**
 * Map dimensions (original scale)
 */
export const MAP_W = 1000;
export const MAP_H = 519;

/**
 * Map dimensions (extended scale for label positioning)
 */
export const MAP_W2 = 4000;
export const MAP_H2 = 4000;

/**
 * Label positioning defaults
 */
export const DEFAULT_H = 55; // Horizontal offset for label leader line
export const DEFAULT_V = 85; // Vertical offset for label leader line
export const DEFAULT_ANGLE = 0; // Default angle in degrees
export const DEFAULT_TURN = 90; // Default turn angle in degrees
